{{ form_start(form) }}
    {{ form_row(form.Titre) }}
      {{ form_row(form.description) }}
      {{ form_row(form.Client) }}
      {{ form_row(form.Categories) }}

    <button class="btn btn-success mt-3">{{ button_label|default('Créer une opération') }}</button>

{{ form_end(form) }}
<a href="{{ path('app_operations_index') }}" class="btn btn-warning mt-3">{% trans %}Revenir à la liste des opérations{% endtrans %}</a>