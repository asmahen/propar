{% extends 'base.html.twig' %}

{% block title %}Operations{% endblock %}

{% block body %}
    <h1>Operations</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ operation.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ operation.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_operations_index') }}">back to list</a>

    <a href="{{ path('app_operations_edit', {'id': operation.id}) }}">edit</a>

    {{ include('operations/_delete_form.html.twig') }}
{% endblock %}
