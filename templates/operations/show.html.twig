{% extends 'base.html.twig' %}

{% block title %}{% trans %}Opérations{% endtrans %}{% endblock %}

{% block body %}
    <h1>{% trans %}Opérations{% endtrans %}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ operation.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ operation.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_operations_index') }}">{% trans %}Retour à la liste{% endtrans %}</a>

    <a href="{{ path('app_operations_edit', {'id': operation.id}) }}">{% trans %}Modifier{% endtrans %}</a>

    {{ include('operations/_delete_form.html.twig') }}
{% endblock %}
